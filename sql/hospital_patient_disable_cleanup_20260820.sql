-- ----------------------------
-- 患者删除语义修复
-- 删除患者统一调整为停用，不再隐藏患者档案。
-- 已经被逻辑删除的历史患者恢复为停用状态，避免页面查不到但患者编码仍被唯一索引占用。
-- ----------------------------

update hospital_patient
set del_flag = '0',
    status = '1',
    update_time = sysdate()
where del_flag = '2';

